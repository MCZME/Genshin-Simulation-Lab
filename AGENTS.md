# Codex 项目协作规则

本文档是仓库级 Codex/AI 协作入口。处理本项目任务时，先阅读本文件，再阅读与任务相关的 `docs/` 文档。

## 项目定位

- 本项目是旧项目的移植与重建，不是简单复制旧代码。
- 当前重点是先稳定架构边界、数据契约、测试与迁移策略，再逐步迁移有效能力。
- 仿真数值和机制行为必须可验证；不要仅凭记忆实现角色、武器、圣遗物或反应细节。

## 必读文档

开始较大改动前，至少阅读：

- [文档入口](docs/文档入口.md)
- [项目决策记录](docs/决策/项目决策记录.md)
- [模块边界设计](docs/架构/模块边界设计.md)

涉及数据或配置时，还要阅读：

- [资产数据库设计](docs/契约/资产数据库设计.md)
- [仿真配置契约与最小闭环](docs/契约/仿真配置契约与最小闭环.md)

涉及 AI 协作、测试、迁移或质量标准时，还要阅读：

- [AI 协作规范](docs/工程/AI协作规范.md)
- [工程规范](docs/工程/工程规范.md)

## 架构硬规则

- `core/` 是纯仿真运行时核心，不依赖数据库、UI、应用层或具体内容实现。
- `core/` 不能 import `assets`、`infrastructure`、`application`、`ui`、`content`。
- `content/` 保存具体角色、武器、圣遗物和 handler 实现，可以依赖 `core/` 与资产数据对象。
- `application/` 负责编排用例，不直接编写具体 SQLite 查询。
- `infrastructure/` 保存 SQLite、文件存储、日志等具体技术实现。
- `analysis/` 只做结果加工、聚合、对比和报告模型，不写库、不画 UI。
- `ui/` 和 `cli/` 通过 `application/` 调用能力，不直接访问 SQLite 或组装仿真核心对象。

## 文档规则

- `docs/` 下文件夹和文件名使用中文。
- 文档正文使用中文。
- 文档路径引用使用相对链接。
- 改动架构边界、配置契约、资产库 schema、结果数据模型或迁移策略时，必须同步更新对应文档。
- 尚未形成结论的内容应明确标注为草案、待确认或待讨论。

## 开发规则

- 优先保持改动范围小，避免顺手重构无关模块。
- 迁移旧项目能力时，先确定行为目标和验收方式，再写实现。
- 复杂数值逻辑优先补 golden case 或最小可复现实例。
- 不把显示名称作为仿真识别依据；资产引用统一使用 `asset_key`。
- `handler_key` 是代码实现绑定入口，不要用显示名称或 `asset_key` 直接绑定行为。
- 缺资产、缺 handler、JSON 格式错误应在组装阶段报错，不延迟到仿真运行中。

## AI 工作流程

1. 先读任务相关文档和现有代码。
2. 判断是否涉及架构、契约、数值或迁移风险。
3. 小步实现，优先沿用现有目录和命名。
4. 同步必要文档。
5. 运行可用检查，并在最终回复中说明检查结果。

遇到以下情况应请求人工确认：

- 改变顶层模块职责或依赖方向。
- 改变 `SimulationConfig`、资产库 schema 或结果库核心结构。
- 引入新的外部依赖、数据源、网络抓取流程或大型工具链。
- 实现尚无可信来源或 golden case 支撑的游戏机制数值。
- 删除或覆盖用户已有改动。

## 验证命令

当前项目使用 `pytest`、`ruff`、`pyright` 作为第一批质量工具。

常用命令：

```powershell
uv run pytest
uv run pytest --cov=genshin_sim
uv run ruff check
uv run ruff format
uv run pyright
```

如果命令不可用，不要伪造结果；在最终回复中说明未运行或失败原因。
