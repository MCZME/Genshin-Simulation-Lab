# 治疗系统实现规划

> 状态：第一轮已完成，当前完成边界为阶段 C。
> 架构职责见[治疗系统设计](../架构/系统/治疗系统设计.md)，公共数据与行为契约见[治疗系统契约](../契约/治疗系统契约.md)。

最后更新：2026-07-13

## 1. 实施目标

建立独立于生命状态的治疗数值结算能力，并通过事件和 `HealthRuntime` 完成单目标治疗闭环。

第一轮只覆盖类型化请求入口，不接入真实 content 或 `ImpactKind.HEAL`。

## 2. 第一轮范围

已完成：

- 多 component 治疗请求、结果和结构化错误。
- 治疗基础值、治疗加成与受治疗加成公式。
- 纯计算 `HealingResolver`。
- `HEALING_RESOLVED` 事件和通用 JSON 持久化。
- `HealingRequestHandler`、生命系统 adapter 和 assembler 暴露。
- 普通、溢出、满血、零治疗和生命提交失败测试。
- 超大整数、聚合溢出和事件引擎错配回归测试。

未包含：

- 真实角色、武器、命座或圣遗物治疗 content。
- 队伍、范围和多目标治疗分发。
- `ImpactKind.HEAL` payload 与 dispatcher 接线。
- 周期治疗、快照、生命之契和生存状态。
- 真实数值 golden case、UI 和 analysis。
- `SimulationConfig`、资产库 schema 或结果库 schema 变更。

## 3. 阶段记录

| 阶段 | 状态 | 交付结果 |
| --- | --- | --- |
| A：模型与公式 | 已完成 | 请求、结果、错误、公式与数值边界测试 |
| B：Resolver 与事件 | 已完成 | 属性解析、`HEALING_RESOLVED`、payload 与持久化 |
| C：生命系统接入 | 已完成 | handler、adapter、assembler 和生命提交闭环 |
| D：真实 content 与 golden case | 待开始 | content adapter、目标契约、真实能力与可信基准 |

第一轮在阶段 C 完成。阶段 D 是独立后续任务，不作为当前完成条件。

## 4. 实现入口

核心实现：

```text
src/genshin_sim/core/systems/healing/
src/genshin_sim/core/events/
src/genshin_sim/core/systems/health/
src/genshin_sim/application/assembly/assembler.py
```

测试入口：

```text
tests/unit/core/systems/healing/
tests/unit/core/events/test_event_types.py
tests/unit/application/assembly/test_assembler.py
tests/integration/infrastructure/results_sqlite/test_results_repository.py
```

阶段 A-C 没有修改：

```text
src/genshin_sim/core/entity_states/health.py
src/genshin_sim/core/entity_states/targets.py
src/genshin_sim/application/config/models.py
资产数据库 schema
结果数据库 schema
```

## 5. 阶段 D 任务

1. 选择一个真实治疗能力并复核机制来源、倍率、固定值、目标、时序和快照规则。
2. 设计 `ImpactKind.HEAL` 到 `HealingRequest` 的 content payload 与目标引用契约。
3. 明确单体、全队和范围治疗如何展开为稳定顺序的单目标请求。
4. 由 content 构造治疗请求，禁止直接修改当前生命。
5. 建立最小真实数值 golden case，并记录输入属性、公式参数、理论治疗、有效治疗和溢出量。

阶段 D 验收：至少一个真实治疗能力可以从运行时影响请求进入治疗 handler，数值具有可信来源并可由 golden case 复现。

## 6. 迁移与资料说明

通用治疗公式参考 [KQM 治疗公式](https://library.keqingmains.com/combat-mechanics/damage/healing)。具体内容接入前仍须重新复核版本相关细节。

旧项目参考入口：

```text
E:\project\Genshin Damage calculation\core\systems\health_system.py
E:\project\Genshin Damage calculation\docs\core\生命值系统.md
E:\project\Genshin Damage calculation\character\character.py
```

旧实现只用于覆盖范围参考，不直接迁移，因为它把治疗公式、生命修改、日志和事件混在同一入口，且缺少可靠的独立 golden case。

## 7. 验证

常用命令：

```powershell
uv run pytest tests/unit/core/systems/healing
uv run pytest tests/unit/core/systems/health
uv run pytest tests/unit/core/events
uv run pytest tests/integration
uv run pytest
uv run pytest --cov=genshin_sim
uv run ruff check
uv run ruff format --check
uv run pyright
```

最近一次验证（2026-07-13）：

- 治疗相关定向测试：`60 passed`。
- 完整测试：`301 passed`。
- 覆盖率测试：`301 passed`，总覆盖率 `89%`。
- `ruff check`：通过。
- `ruff format --check`：本次 Python 变更文件通过；全仓仍有既有格式差异。
- `pyright`：`0 errors, 0 warnings`。
- `git diff --check`：通过。

## 8. 完成判定

阶段 A-C 当前满足：

- 治疗公式独立于生命状态并生成不可变理论结果。
- 多 component、负加成、零治疗和溢出行为可验证。
- resolver 不依赖事件或生命系统。
- handler 固定按“理论结算事实 -> 生命提交”编排，并保证共用事件引擎。
- 满血和零治疗不产生虚假生命变化事件。
- 治疗事件无需结果库 schema 变更即可持久化。
- assembler 暴露的 handler 可以实际提交角色生命并产生有序事件。

阶段 D 的真实 content 和 golden case 完成后，再更新本规划状态与能力矩阵。
