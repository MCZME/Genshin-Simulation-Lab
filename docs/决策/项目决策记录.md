# 项目决策记录

> 记录当前项目重建过程中的高层决策、设计文档索引与后续待讨论事项。
> 详细方案按主题拆分到独立文档中，本文只保留结论。

最后更新：2026-07-03

## 1. 设计文档索引

- [资产数据库设计总结](../契约/资产数据库设计.md)
- [新项目模块边界设计](../架构/模块边界设计.md)
- [当前项目架构流程图](../架构/当前项目架构流程图.md)
- [仿真配置契约与最小闭环](../契约/仿真配置契约与最小闭环.md)
- [迁移阶段安排](../工程/迁移阶段安排.md)

## 2. 重建目标

当前项目将进入归档与迁移阶段：

1. 归档当前项目。
2. 创建新项目。
3. 确定新项目架构与管理模式。
4. 逐步迁移当前项目中仍然有效的内容到新项目。

新项目不是简单重写旧代码，而是在保留有效概念的基础上重新整理边界、数据层和应用形态。

## 3. 已确认决策

### 3.1 产品方向

- 新项目核心仍然是仿真引擎。
- 应用端从简单伤害计算器扩展为更广义的分析工具。
- 分析能力围绕仿真结果、战斗复盘、参数扫描和结果对比逐步展开。

### 3.2 技术与项目管理

- UI 继续使用 Flet，但具体 UI 方案暂缓设计。
- 新项目采用单仓库单包。
- Python 包名暂定 `genshin_sim`。
- 使用 `src/` 布局。
- 使用 `uv` 管理项目。
- `pyproject.toml` 作为项目配置入口。
- 质量工具暂定为 `ruff`、`mypy` 或 `pyright`、`pytest`。

### 3.3 数据源

- 新项目改为本地数据库。
- 静态资产库第一版使用 SQLite。
- 运行期结果库第一版继续使用 SQLite。
- 原 MySQL 服务器已销毁，基础资产数据需要重新获取。
- 数据获取方案已有旧项目基础，可通过改造后继续使用。

### 3.4 基础资产数据库

- 资产库作为只读生成产物存在。
- 不设置 `raw_assets` 表。
- 采用结构化索引表与 JSON Payload 混合模型。
- `asset_key` 使用 `{asset_type}:{source_id}` 格式。
- `handler_key` 与 `asset_key` 分离，用于绑定仿真代码实现。
- 技能倍率使用 `scaling_json`。
- 武器、天赋、命座、圣遗物等复杂效果使用 `params_json`。
- `sim-core` 不直接访问 SQLite，而是依赖 `AssetRepository` 协议。

详细方案见：[资产数据库设计总结](../契约/资产数据库设计.md)。

### 3.5 基础资产范围

- 角色、武器、圣遗物等数据进入基础资产数据库。
- 敌人基础抗性/等级信息第一版不纳入基础资产数据库。
- 多语言文本第一版不纳入基础资产数据库。
- 复杂机制行为由仿真代码实现；数据库只保存参数和处理器标识。

### 3.6 模块边界

新项目在 `src/genshin_sim/` 下采用以下顶层模块：

- `core/`
- `content/`
- `assets/`
- `infrastructure/`
- `application/`
- `analysis/`
- `ui/`
- `cli/`

核心原则：

- `core/` 是纯仿真运行时核心，不依赖数据库、UI、应用层或具体内容实现。
- `content/` 保存具体角色、武器、圣遗物和 handler 实现。
- `application/` 负责编排 UI/CLI、资产库、内容实现、仿真核心、结果库和分析能力。
- `infrastructure/` 保存 SQLite、文件存储、日志等具体技术实现。
- `analysis/` 负责结果加工、聚合、对比和报告模型。
- `ui/` 作为 Flet 入口，通过 `application/` 调用能力；具体 UI 设计暂缓。

详细方案见：[新项目模块边界设计](../架构/模块边界设计.md)。

### 3.7 仿真快照与结果库

- 新项目允许 `sim-core` 生成快照。
- 快照用于复盘、审计、分析和调试。
- 快照不是资产数据，也不是项目配置文件。
- 快照生成属于 `core/`，快照持久化属于 `infrastructure/`。
- 运行期结果库与基础资产库职责分离。
- 所有仿真都保存完整结果数据，不区分 `summary_only` 与 `recorded` 模式。
- 运行期结果库不重新展开设计，直接迁移并整理当前项目中已经较完善的 SQLite 持久化、事件流与投影表方案。

### 3.8 重设计与直迁分层

当前迁移阶段按内容类型分层处理：

- 资源数据库：重新实现。
- 模拟架构中的输入轨迹与动作解释：重新设计并重新落位。
- UI：完全重设计。
- 其余游戏逻辑实现：优先直接迁移到新项目对应模块，但仍需符合新边界和测试要求。

## 4. 旧项目中值得保留的概念

- `SimulationContext`
- `EventEngine` / `GameEvent`
- `Simulator`
- `ActionCommand`
- `SimulationAssembler`
- `ResultDatabase` + `DataProjector`
- `BatchProject` / `BatchNode` / `MutationRule`
- Flet MVVM 分层思路

## 5. 待讨论事项

### 5.1 仿真配置契约

已确认初步方向：

- 使用 `SimulationConfig` 描述一次仿真。
- 顶层结构包含 `meta`、`team`、`scene`、`input_trace`、`rules`、`run_options`。
- 配置引用资产时统一使用 `asset_key`。
- `input_trace` 记录按键输入事实，不使用角色名称。
- 敌人目标信息直接写入 `scene`，第一版不进入资产库。
- `run_options` 不包含 `mode`。
- 所有仿真都保存完整结果数据。

详细方案见：[仿真配置契约与最小闭环](../契约/仿真配置契约与最小闭环.md)。

### 5.2 应用端 MVP

第一版 MVP 定为最短闭环：

- 创建或编辑一个 `SimulationConfig`。
- 队伍/角色构建配置。
- 输入轨迹编辑。
- 运行一次仿真。
- 保存完整仿真结果数据。
- 仿真结果摘要。

第一版暂不强制包含：

- 完整战斗过程复盘。
- 参数扫描。
- 多结果对比。
- 配置模板库。
- 复杂 UI 工作区。

### 5.3 CLI 与工具脚本

已确认第一版命令：

- `genshin-sim assets build`
- `genshin-sim assets validate`
- `genshin-sim assets info`
- `genshin-sim config validate path/to/config.json`
- `genshin-sim run path/to/config.json`
- `genshin-sim results list`
- `genshin-sim results inspect <session_id>`

原则：

- CLI 只作为开发、数据构建、调试和自动化入口。
- CLI 调用 `application/` 服务，不重复实现仿真组装逻辑。
- 第一版使用标准库 `argparse`。

详细方案见：[新项目模块边界设计](../架构/模块边界设计.md)。

### 5.4 测试与迁移策略

需要细化：

- `tests/unit/`、`tests/integration/`、`tests/golden/` 的职责。
- golden case 的保存格式。
- 数值可信度验证方式。

已确认原则：

- 测试不能直接照搬旧项目测试文件。
- 测试必须基于新项目架构、配置契约和模块边界重新编写。
- 旧项目测试只作为行为覆盖范围与数值预期参考。
- 旧项目能力的总体迁移阶段见：[迁移阶段安排](../工程/迁移阶段安排.md)。

### 5.5 UI 重设计

UI 需要大改，暂不做具体设计。

当前仅确认：

- UI 继续使用 Flet。
- UI 通过 `application/` 调用能力。
- UI 不直接访问 SQLite。
- UI 不直接组装仿真核心对象。

## 6. 下一步

下一步优先细化测试与 golden case 策略，并按迁移阶段安排启动第一批低风险核心能力迁移。
