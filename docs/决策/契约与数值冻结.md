# 决策记录：契约与数值冻结

> 状态：持续维护。
> 本文是项目决策记录的主题分区之一，收录契约冻结、关键数值与真值来源决策。
> 条目编号自 `D-001` 起连续分配，跨分区稳定，不重排、不复用；条目正文始终描述当前生效结论，决策演变在条目内以变更记录维护。
> 状态取值：`生效`、`生效（部分被 D-XXX 修订）`、`已被 D-XXX 替代`、`后置待定`。
> 决策记录的定位、写入门槛与条目模板见 [AGENTS.md](../../AGENTS.md)；正式文档索引见[文档入口](../文档入口.md)。

## 条目索引

- [D-017 元素反应契约冻结](#d-017-元素反应契约冻结)：生效
- [D-018 月反应独立契约冻结](#d-018-月反应独立契约冻结)：生效
- [D-021 效果实现状态标记与 handler 绑定](#d-021-效果实现状态标记与-handler-绑定)：生效
- [D-068 星超导契约冻结](#d-068-星超导契约冻结)：生效
- [D-069 星扩散契约冻结](#d-069-星扩散契约冻结)：生效
- [D-070 状态效果队伍作用域主体](#d-070-状态效果队伍作用域主体)：生效
- [D-071 星扩散跳跃能力 Buff 的形态与消耗链路](#d-071-星扩散跳跃能力-buff-的形态与消耗链路)：生效（部分被 D-072 修订）
- [D-075 反应公式专属伤害修饰项阶段](#d-075-反应公式专属伤害修饰项阶段)：生效
- [D-077 西风长枪顺风而行数值与判定冻结（2026-09-19）](#d-077-西风长枪顺风而行数值与判定冻结2026-09-19)：生效

### D-017 元素反应契约冻结

> 状态：生效

- 普通反应由本次元素、ICD 后实际元素量和目标 Aura 决定；碎冰额外读取稳定 `strike_type=BLUNT` 证据，来源种类、武器显示类型和布尔“允许反应”开关不参与。
- 反应处理当前 Impact 时只能调整该 Impact 已携带的关联伤害；新增伤害一律成为同帧下一 settlement round 的子 Damage Impact。
- 增幅/剧变：Profile 拆为 `AmplifyingReactionProfile | TransformativeReactionProfile` 强类型联合；超载、超导使用独立稳定 key 与 Gate，effect group 在下一 settlement round 执行。
- 燃烧：普通燃烧火草建立时创建 `2 GU` 燃元素与 `BurningState`；周期火每 `15` 帧造成 `0.25` 倍率火元素剧变伤害，普通火每 `120` 帧施加 `1 GU`，不进入 Aura ICD。
- 激化：原激化按草雷 `1:1` 实际消费创建 `QUICKEN`；超激化/蔓激化不消费激元素，在通用公式的激化附加区结算（`CatalyzeReactionInput`），倍率 `1.15/1.25`，精通公式已固定。
- 绽放：普通绽放/绽放爆炸/超绽放/烈绽放，水与类草 `2:1` 消费；草原核持续 `360` 帧、每队最多五枚，容量淘汰引爆最老核心；`bloom_family` 共享 `30` 帧两次 Gate，三种伤害分类独立。

各反应稳定字段、key 和验收入口见对应反应契约与[元素反应公共模型契约](../契约/元素反应/公共模型契约.md)。

### D-018 月反应独立契约冻结

> 状态：生效

- 月绽放：独立 `ReactionKey`、handler 与方向，capability 准入加严格水草签名，与普通绽放互斥（触发时独占）；反应本身不产生伤害，草露由 ReactionStore 拥有，角色月曜伤害走 `CHARACTER_DIRECT`；生产公式已注册。
- 月感电：独立 key 与严格水雷签名，与普通感电互斥（触发时独占）；occurrence 不消费水雷（后手按 `20%` 损耗保持共存）；`LunarStormCloudState` 生命周期 `360` 帧、每 `15` 帧攻击、逐目标 Gate 并消费 `0.4 GU`；复合月曜伤害按 `0.60/0.30/0.05` 加权。
- 月结晶：独立 key 与严格水岩签名，只替换水结晶方向；场上最多三枚月笼、共享累计器、每三次月结晶触发一次谐奏；谐奏复合月曜伤害不产生元素附着。
- 三条月反应均已有生产 bootstrap 与 golden case；未确认项（可回放随机、岩造物干扰、精确倍率等）记录在正式契约，不改变生产注册状态。

详细见[月绽放反应设计](../架构/系统/元素反应/反应/月绽放反应设计.md)、[月感电反应设计](../架构/系统/元素反应/反应/月感电反应设计.md)、[月结晶反应设计](../架构/系统/元素反应/反应/月结晶反应设计.md)及对应契约。

### D-021 效果实现状态标记与 handler 绑定

> 状态：生效

- `handler_key` 只表达“是否已为该资产/效果添加代码条目”，实现程度由 `HandlerImplementationStatus`（`IMPLEMENTED` / `EMPTY` / `UNIMPLEMENTED` / `NOT_ADDED`）标记表达，不由键名表达。
- 资产库 handler 绑定通过 `genshin-sim assets set-handler` / `reset-handler` /
  `show-handlers` / `sync-handlers` 维护命令管理；可通过 `--manifest` 写回本地
  标准资产 manifest，或由 `sync-handlers` 批量补同步，重建数据库后无需重新应用；
  设置时校验目标存在、键非空、幂等与 handler 已注册。

详见[资产数据库契约](../契约/资产数据库契约.md#111-handler-绑定维护命令)。

### D-068 星超导契约冻结

> 状态：生效

- 独立 key 集合：`reaction.stellar_conduct`、`reaction_handler.stellar_conduct`、`reaction_capability:stellar_conduct`、`reaction_state.polestar_field`、`reaction_state.stellar_conduct_counter`、`reaction_spatial_profile.polestar_field`、`damage_formula.stellar_reaction`；命名采用官方英文 `Stellar-Conduct` / `Polestar Field` 的 snake_case，不使用社区俗称 `star_superconduct`，不复用 `lunar_*`。
- 星超导候选 `selection_priority=110`：capability 与角色来源齐备时排他替代普通超导，否则回退普通超导；冰雷按 `min(incoming, aura)` `1:1` 消费，两个方向签名与普通超导一致。
- 星超导没有公共反应伤害：occurrence 只声明极星辉域计划。领域 State/Space binding 与雷暴云一致；域内重触发（X/Z `<= 12`，忽略 Y 轴）只刷新生命周期、保留实例与圆心，域外重触发替换旧领域；同一队伍至多一个领域。
- 附着计数按队伍共享，不绑定 Field 实例；`240` 帧窗口到期结算快照层数、清空窗口并推进；新领域生成时排除触发攻击身份，只影响当前窗口；不建立"前 4 秒"预热窗口，`0` 层直伤星烁基础系数为 `1`。
- 星烁伤害使用独立 `damage_formula.stellar_reaction` 与强类型 `StellarReactionDamageInput`/`StellarReactionDamageResolution`，首期 `character_direct`，`reaction_composite` 留待星扩散；不接受普通 `DamageModifierTerm` 与通用反应输入混入。
- 数值冻结状态：领域持续 `420` 帧（约 7 秒）、计数窗口 `240` 帧、领域半径 `12` 为当前实现基线，未作正式数值冻结；冻结状态与验收见[星超导反应契约](../契约/元素反应/反应/星超导反应契约.md)。
- 角色直伤星超导 content 技能模组（角色资产接入）为后续切片；未确认项记录在正式契约非目标。

原因：把星超导建为独立 Definition 而非普通超导 Rule 的条件分支，可复用月相反应的选择优先级与回退模式，并保持普通超导语义不变；早期"星超导周期结算产生公共反应伤害"的规划已被资料修正（星超导不存在反应星烁伤害），周期结算的公共结果改为层数快照与系数可查询性；星烁公式独立于通用反应区，是因为直伤星烁的星烁基础增伤、大权区、羽毛区等区间无法由普通 `DamageModifierTerm` 表达。

承载：[星超导反应设计](../架构/系统/元素反应/反应/星超导反应设计.md)、[星超导反应契约](../契约/元素反应/反应/星超导反应契约.md)（两者均记录资料基线与来源链接）。

变更记录：2026-09-12 新建条目，冻结首期星超导 key 集合、资格与消费语义、领域/计数状态模型、层数系数映射与星烁公式请求边界。
- 2026-09-15 收紧实施状态表述：辉映·星烁 Buff 接线、领域减抗复用、附着上游与星烁结算器分支已落地，仅角色直伤技能模组保留为后续切片；数值冻结状态统一指向星超导契约。
- 2026-09-17 新增两个直伤主攻击标签 `星超导冰` / `星超导雷`（按伤害元素拆分），`reaction.stellar_conduct` 不再兼任伤害标签、只保留反应身份；见 D-076。

### D-069 星扩散契约冻结

> 状态：生效

- 独立 key 集合：`reaction.stellar_swirl`、`reaction_handler.stellar_swirl`、`reaction_capability:stellar_swirl`、`reaction_state.stellar_swirl_vortex`、`reaction_spatial_profile.stellar_swirl_vortex`、`buff.reaction.stellar_swirl.radiance`、伤害标签 `星扩散风` / `星扩散冰`、damage kind `reaction_damage.stellar_swirl_wind` / `reaction_damage.stellar_swirl_ice`、去重 Gate `reaction_gate.stellar_swirl.wind`；复用 `damage_formula.stellar_reaction` 并启用 `reaction_composite` 模式。命名采用官方英文 `Stellar-Swirl` 的 snake_case，不使用社区俗称 `star_swirl`，不复用 `lunar_*`。
- 候选 `selection_priority=110`：capability 与角色来源齐备时排他替代普通扩散，否则回退；本期只注册风命中冰一个方向，消费与普通扩散一致（`incoming_consumed = min(incoming, aura×2)`、aura 侧减半）；风元素自附着方向依赖 `AuraKind` 扩展 ANEMO，明确后置，反向方向 key 仅预留不注册。
- 星辉风旋为 `battle` 作用域全场唯一的 State/Space binding 对象：锚定首个反应目标位置、等级 `1~6`、`180` 帧自动爆炸、6 级立即爆炸、升级不移动锚点也不刷新爆炸计时；6 级爆炸后同帧后序目标创建等级 `1` 新风旋，账本与伤害源窗口无跨风旋继承。
- 伤害源与参与者分层：星扩散·风伤害源 = 触发者，参与者 = 触发者 + 冰贡献在反应发生时活跃且被本次反应消费的角色（集合语义、不要求参与者持有 capability）；星扩散·冰无反应触发者，伤害源 = 生命周期内最后一次星扩散·风的触发者（含被去重挡掉与 6 级同帧事件），参与者 = 各风参与者并集；伤害源必在参与者集合内；结构性至多 4 名，兜底按首次参与帧升序保留。
- 风伤害去重是伤害结算层规则：去重只拦截伤害实例（Gate 窗口 `30` 帧基线、至多 1 次），不影响反应发生、元素消费、风旋等级累加、参与者计入与伤害源认定。
- 星扩散·风单体、星烁基础系数 `0.75`、可暴击、不附着元素；星扩散·冰爆炸按等级使用圆柱 AOE（半径 `6/8`、高度 `4` 仅作强类型记录）、系数 `2/3`、可暴击、对命中目标附着 `1U` 冰元素并归因于伤害源，后续反应触发者随之。
- 反应星烁复合公式：逐参与者以自身等级的 `reaction_base_value` 替换 `scaling_value` 结算，按折前伤害稳定排序取前 `4` 名，权重按名次固定 `0.60/0.30/0.05/0.05` 且不按参与人数重归一化（与月曜复合实现同构），逐参与者独立暴击决策；聚合与组分审计写入 `StellarReactionDamageResolution`。
- 辉映·星扩散 Buff：`480` 帧基线、每次风触发统一刷新，直伤星扩散基础系数词条固定 `1.0`；目标集合由辉映资格证据决定，角色与 key 粒度在角色资产接入阶段定案；与辉映·星超导可共存，同一角色同时满足两种状态条件时星超导优先（能力层读取优先级执行，Buff 层不互斥）。
- 数值冻结状态：风旋 `180` 帧、辉映 `480` 帧、去重窗口 `30` 帧为当前实现基线；爆炸 AOE 半径/系数、四项权重与 `1U` 附着为来源化数值；冻结状态与验收见[星扩散反应契约](../契约/元素反应/反应/星扩散反应契约.md)。
- 切片边界：风旋爆炸命中角色的跳跃能力 Buff 的形态、命中分流与"起跳即消耗"链路改由 [D-071](#d-071-星扩散跳跃能力-buff-的形态与消耗链路) 承载，本条目不再覆盖；仍为后续切片的是该 Buff 的量化口径（"提高跳跃能力"如何提高）与角色起跳动作接线，以及角色直伤技能模组。

原因：星扩散的核心新语义是"伤害源"与"伤害计算参与者"分层和风旋生命周期对象，参与者判据以本项目 Aura 消耗模型为真值（反应发生时贡献活跃等价于被消费），不引入外部页面的"元素子线程"猜想结构；风旋复用草原核的到期爆炸通道与星超导的 State/Space binding 模式，作用域采用 `battle` 是因为资料明确风旋全场唯一，不等同星超导的 `player_team`；去重复用既有 `ReactionDamageGate` 机制；复合公式逐参与者形态沿用月曜公式已验证的实现结构。早期"星扩散按目标各建风旋"与"去重同时约束等级累加"两种理解均被资料与讨论否定。

承载：[星扩散反应设计](../架构/系统/元素反应/反应/星扩散反应设计.md)、[星扩散反应契约](../契约/元素反应/反应/星扩散反应契约.md)（两者均记录资料基线与来源链接）。

变更记录：
- 2026-09-12 新建条目，冻结首期星扩散 key 集合、资格与消费语义、风旋状态模型、伤害源与参与者账本规则、风/冰伤害与去重、反应星烁复合公式请求边界及辉映·星扩散 Buff 契约。
- 2026-09-14 从"后续切片"表述中移除"爆炸冲击 Impact 语义接线"——该语义属 Impact/命中层，不在星扩散契约范围，契约以非目标条目保留。
- 2026-09-17 伤害标签由 `reaction.stellar_swirl.wind` / `reaction.stellar_swirl.ice` 改为 `星扩散风` / `星扩散冰`，反应身份键 `reaction.stellar_swirl` 不变；见 D-076。
- 2026-09-14 基线数值与切片边界拆成两条；原"风旋爆炸命中角色的跳跃能力为后续切片"的结论**已被 D-071 推进**：该切片收窄为量化口径与角色起跳动作接线，形态与消耗链路改由 D-071 承载，本条目不再覆盖。
- 2026-09-15 收紧实施状态表述：删除"机制首期由测试构造"等实施细节，数值冻结状态统一指向星扩散契约。

### D-070 状态效果队伍作用域主体

> 状态：生效

- `AttributeSubjectKind` 增加两个队伍作用域 kind：

  | kind | 作用范围 | 挂载身份 |
  | --- | --- | --- |
  | `team` | 队伍全部角色 | `AttributeSubjectRef.team(team_ref)` |
  | `active_character` | 只作用于当前场上角色 | `AttributeSubjectRef.active_character(team_ref)` |

- 两个 kind 的 `entity_id` 都是队伍稳定作用域 id（例如 `player_team`），不是角色战斗实体 id，也不是空间实体 id；作用范围差异只由 kind 表达。属性主体 kind `active_character` 与空间实体 kind 同名但不同域，不互相推导。
- 队伍作用域 kind 在定义规则上与 character / target 完全一致，不附加额外限制：`marker_only` 与 attribute modifiers 的互斥规则照旧，带 attribute modifiers 的定义照常产出 `BuffAttributeModifierProvider`。
- 队伍作用域词条经属性侧投影可见：公共属性定义全部接受 `team` / `active_character` 为 `owner_kinds`，角色侧属性（如 `stat.atk.*`、`bonus.shield.strength`）同样接受队伍作用域主体，只是不接受 `target`；`BuffAttributeModifierProvider` 在查询角色主体时除精确匹配外，追加该角色所属队伍的 `team` 记录与（当该角色为当前场上角色时）`active_character` 记录；投影是只读展开，`target` 主体与角色主体之间不互相投影，不引入主体继承或下钻。
- 投影所需的队伍归属与场上判定由装配期注入的窄只读端口 `TeamScopeProjectionPort` 提供（适配 `TeamRuntimeState`），不在属性或 Buff 领域硬编码队伍作用域 id；未注入端口时不投影。
- 冲突仍以 `(target_ref, conflict_key)` 判定：同一 conflict key 在 `team`、`active_character`、角色与目标主体之间互不冲突；同一主体上的刷新兼容性照旧要求 definition、target、applier 与 source context 一致。
- 消耗型效果使用既有 `BuffRemovalReason.CONSUMED` 的显式移除请求表达，不新增生命周期状态。
- 消费方按对应主体精确查询后，再自行与"该角色是否符合该 kind 的作用范围"组合；Buff 领域不感知场上角色，也不做主体迁移。唯一例外是属性 provider 的队伍作用域投影：它按装配期注入的端口把队伍作用域记录展开到角色面板，仍是属性侧只读行为，不改变 Buff 领域读取语义。

原因：队伍级效果此前只能展开成每个角色一份（元素共鸣与辉映 Buff 均为 `target_kinds={CHARACTER}`），而"作用于整队"与"只作用于当前场上角色"是两种不同语义——前者与场上是谁无关，后者是位置级效果、切人后应由新前台接管；逐角色复制这两种语义都会引出"切人后效果归属谁"的迁移问题。把槽位做在队伍系统（`TeamRuntimeState`）会形成与 `BuffRuntime` 并行的第二套生命周期、审计与结果库投影机制，并违反"不得为了复用把不同机制塞进通用 Store"的硬规则，因此改为在 Buff 系统内增加两个队伍作用域主体，复用既有全部机制。

承载：[状态效果系统契约](../契约/状态效果系统契约.md)第 19 节与第 30 节、[状态效果系统设计](../架构/系统/状态效果系统设计.md)第 6 节、[属性系统契约](../契约/属性系统契约.md)第 2.1 节、第 4 节与第 6.1 节、[UI API 契约](../契约/ui/UI%20API%20契约.md)第 8 节帧状态端点。

变更记录：
- 2026-09-13 新建条目，冻结队伍作用域主体身份、冲突隔离与读取边界。
- 2026-09-14 由单一 `team` 主体扩展为 `team` + `active_character` 两个 kind，明确"作用于整队"与"只作用于当前场上角色"是两种不同语义。
- 2026-09-14 撤销两项附带设计：一是删除 `TEAM_SCOPE_SUBJECT_KINDS` 常量，不把"两个队伍作用域 kind"固化成共享分类；二是删除"队伍作用域定义必须 `marker_only`"的定义期闸门，队伍作用域 kind 的定义规则与 character / target 完全一致。
- 2026-09-14 落地队伍作用域属性投影：放开全部公共属性接受队伍作用域主体，`BuffAttributeModifierProvider` 按队伍作用域投影端口把队伍记录展开到角色面板；原"词条不可见、原因在属性侧未注册 owner kind"的结论被本条替代。
- 2026-09-14 帧状态投影按同一口径展开队伍作用域 Buff：结果侧帧状态折叠把 `team` 主体记录投影到队伍内每个角色的 `buffs` 列表，把 `active_character` 主体记录投影到当前场上角色；投影是展示层只读展开，领域侧读取语义不变。

### D-071 星扩散跳跃能力 Buff 的形态与消耗链路

> 状态：生效

- 风旋爆炸命中角色后的跳跃能力 Buff（`buff.reaction.stellar_swirl.jump_boost`）挂**位置级队伍作用域主体** `AttributeSubjectKind.ACTIVE_CHARACTER`（`entity_id` 为队伍稳定作用域 id `player_team`），**不挂**精确角色主体 `CHARACTER`：跳跃只有前台角色能执行，位置级语义天然表达"谁在前台谁享受、切人后由新前台接管"，且不引出"命中时是谁就锁死给谁"的迁移问题。
- Buff 形态为 `marker_only`，不声明任何属性词条；`REFRESH` + `REPLACE_LATEST`、至多 `1` 层、存续 `360` 帧（约 6 秒基线）。"提高跳跃能力"没有任何来源化的量化口径（跳跃高度、倍率、体力消耗、二段跳均无数值），位移设施也没有跳跃能力模型可写，因此**不冻结任何数值**，只做存在标记。
- 命中判定与挂载身份分离：爆炸 selection 显式声明 `reaction_target.hostile_effect` 资格策略，不继承 `bloom_damage` 默认值；范围内角色在 Effect 循环内按 `SELF` 关系被挡下（不受冰伤害、不被附着），跳跃 Buff 改由协调器在 Effect 循环**外**的独立通道申请。这样"爆炸不自伤"由资格意图表达，而不是依赖 `StellarReactionDamageImpactEffect` 恰好不在 bloom 自伤白名单的 Effect 类型联合里。
- 消耗时机为**起跳那一刻**而非落地：Buff 在起跳当帧仍可被读取（用于提高本次跳跃），由跳跃动作自身在解析时以 `BuffRemovalReason.CONSUMED` 显式移除，不新增生命周期状态（归属修订见 D-072）。
- 位移设施（`core/movement`）只提供位移入口：`movement` impact 的 `jump.upward_velocity` → `start_jump` 置入向上初速度并推进运动，初速度由调用方给出，设施不内置任何角色跳跃数值；设施不认识 Buff，也不发布起跳事实。
- 本期**不接线**：角色跳跃的速度/高度资料缺失，接内容必然引入无来源数值；角色跳跃动作与其 impact factory 未建，位移入口只由单元测试驱动。读写通道本身已具备（D-073）。

原因：资料只给出"风旋爆炸命中角色后，提高其未来 6 秒跳跃能力；完成一次跳跃后该效果消失"这一句话，命中范围、精确帧数与"跳跃能力"的口径全部未来源化；过早冻结数值会让后续 golden case 建立在猜测上。选位置级主体而非精确角色，是因为空间侧只有 `SpatialEntityKind.ACTIVE_CHARACTER` 一种角色实体（后台角色没有空间实体），"爆炸命中角色"必然是前台角色，与位置级挂载自洽；精确角色主体还会引出"Buff 给谁、切人后归谁"的迁移问题。起跳即消耗而非落地才消耗，是因为 Buff 的语义是"提高本次跳跃"，必须在起跳那一帧仍可读取；同时"完成一次跳跃"在没有连续跳跃状态模型时可判定的最小单位就是那次跳跃动作本身。Movement 不认识 Buff、Buff 不认识 Movement 是系统自治的硬规则，而跳跃动作本身就是消费方，因此消耗由动作侧自持，不再另加协调器（见 D-072）。

承载：[星扩散反应设计](../架构/系统/元素反应/反应/星扩散反应设计.md)第 2 节、第 7 节与第 10 节、[星扩散反应契约](../契约/元素反应/反应/星扩散反应契约.md)第 9 节与第 11 节；队伍作用域主体语义见 [D-070](#d-070-状态效果队伍作用域主体)，星扩散冻结基线见 [D-069](#d-069-星扩散契约冻结)。

变更记录：
- 2026-09-14 新建条目，定案跳跃能力 Buff 的挂载身份、标记形态、爆炸命中分流、起跳消耗时机与 Movement 事实通道边界。
- 2026-09-14 消耗链路归属由 D-072 修订：删除 `core/coordination/movement` 起跳消耗协调器与 `MovementFact.JUMPED` / `MOVEMENT_JUMPED` / `MovementJumpRecord` 事实族，消耗改归角色跳跃动作自身，位移设施只保留 `movement.jump.upward_velocity` 位移入口。挂载身份、标记形态与爆炸命中分流结论不变。

### D-075 反应公式专属伤害修饰项阶段

> 状态：生效

- `DamageModifierStage` 新增两个**公式专属**阶段，与原有 15 个直伤槽位阶段并列：

  | Stage | 值 | 归属公式 | 消费位置 |
  | --- | --- | --- | --- |
  | `TRANSFORMATIVE_REACTION_BONUS_ADD` | `transformative_reaction_bonus_add` | 剧变公式 | 并入 `reaction_bonus`，精通区加算括号 |
  | `STELLAR_REACTION_BONUS_ADD` | `stellar_reaction_bonus_add` | 星烁公式 | 并入 `stellar_bonus`，精通区加算括号 |

- **粒度按公式切分**：每公式一个专属阶段，两公式的 `allowed_modifier_stages` 白名单互不重叠。不采用"反应类共用一个阶段"的方案——每公式一个语义边界清晰，各白名单互不牵连，新增反应类型时不会误放行。
- 剧变公式 `TRANSFORMATIVE_ALLOWED_MODIFIER_STAGES` 与星烁公式 `STELLAR_ALLOWED_MODIFIER_STAGES` 从空集改为各含本公式专属阶段；`GENERAL_ALLOWED_MODIFIER_STAGES` 与月曜公式白名单**不含**这两个阶段。
- 专属阶段以 `fsum` 汇总后**并入**对应基线字段（`reaction_bonus` / `stellar_bonus`），**不替换**调用方冻结的基线值；改动不触及防御策略（剧变仍 `approximate_unity`）、不暴击口径与其余乘区顺序。
- 越界一律拒绝：普通阶段进入反应公式、反应专属阶段进入通用/月曜公式、以及两反应公式互换阶段，都在入公式前由 `_validate_formula_stages` 抛出 `DamageProviderViolationError`，不做静默忽略。公式体内原有的 `raise` 退化为防御性兜底。
- 契约措辞澄清：[伤害系统契约](../契约/伤害系统契约.md)与[星超导反应契约](../契约/元素反应/反应/星超导反应契约.md)中"不接受**普通** Damage modifier stage"的"普通"是限定词，只排除直伤槽位词条，不排除公式专属阶段；星超导契约原禁止项"不通过普通 `DamageModifierTerm` 伪造"继续约束星烁基础系数、基础增伤、大权区、羽毛区与擢升，**星烁增伤**一项除外——它额外接受专属阶段贡献，强类型字段仍是载体。

原因：内容侧需要表达"装备者对某类反应伤害提升固定比例"的常驻加成（本批两套圣遗物均涉及）。这类效果作用于**反应伤害**而非角色直伤，因此不能走直伤槽位词条；而反应公式的反应加成位此前只能由机制常量或调用方冻结值写入，内容侧无贡献入口。改为属性通道会与剧变契约"等级系数、元素精通与精通加成在 occurrence 时冻结，Damage 不查询来源属性"直接冲突；新增通用修饰项传递框架则成本远高于收益。补专属阶段复用现成的 `allowed_modifier_stages` 白名单、`_validate_formula_stages` 校验、`_sum_terms` 汇总与 `DamageModifierProvider` 协议，无新概念，且内容侧与既有 `CRIT_RATE_ADD` 用法完全同形。

承载：[伤害系统契约](../契约/伤害系统契约.md)第 3.2 节与第 6.3 节、[星超导反应契约](../契约/元素反应/反应/星超导反应契约.md)第 8 节、[伤害系统设计](../架构/系统/伤害系统设计.md)第 6 节与第 7 节。

变更记录：
- 2026-09-17 新建条目，定案反应公式专属修饰项阶段的命名、粒度、消费位置与越界拒绝口径，并澄清契约中"普通"限定词的准确含义。

### D-077 西风长枪顺风而行数值与判定冻结（2026-09-19）

> 状态：生效

- 西风长枪（`weapon:13407`）被动「顺风而行」产球形态冻结为 `pickup_kind=particle`、`element=clear`、`count=3`，即**3 个无元素微粒**；精炼只改变产球频率，不改变数量。
- 触发四条条件必须同时成立：本次伤害命中敌方目标（不是角色目标）、本次伤害实际大于 0、本次攻击存在暴击、装备者在场。判定粒度为「每个造成伤害的命中 × 每个被命中的敌人」各自独立判定。
- 触发概率与触发间隔**不在内容代码中改写**，由内容工厂从资产效果参数（`weapon:13407` 效果 `passive:113407` 的 `params.components`）按精炼读取，帧率基准 60 帧/秒：

  | 精炼 | 触发概率 | 触发间隔 | 间隔帧数 |
  | --- | --- | --- | --- |
  | R1 | 60% | 12s | 720 |
  | R2 | 70% | 10.5s | 630 |
  | R3 | 80% | 9s | 540 |
  | R4 | 90% | 7.5s | 450 |
  | R5 | 100% | 6s | 360 |

- `components` 的下标语义（第 0 项为触发概率、第 1 项为间隔秒数）是**西风长枪内容包自己的声明**，不是资产契约的一部分：资产契约只规定 `components` 的 `values` 必须是数组、且长度等于精炼范围（[资产数据库契约](../契约/资产数据库契约.md)第 8 节），不规定各项的含义。解读责任在内容侧，见 D-080。
- 精炼范围同样取自资产参数（`refinement_min` / `refinement_max`），不在内容代码中写死；精炼越界、参数缺失或取值非法都在组装阶段失败，不静默回退默认值。
- 描述文本中的「恢复 6 点元素能量」**不设**独立直接回能分支：3 个无元素微粒按元素能量系统既有规则自然得到场上 `3 × 2 × 1.0 = 6`（充能效率乘算前）。因此 `element` 取无元素而不是装备者元素——按装备者元素建模会得到场上 9 点，与描述不符。
- 载体延迟 `travel_frames` 的 `[20, 50]` 是**工程约定，不是游戏真值**，不得在后续契约或结论中引用为机制数据。
- 绑定键冻结：武器行 `weapon.favonius_lance`，效果行 `weapon.favonius_lance.passive`（空实现）。行为落在武器内容单元而不是效果单元，因为 `WeaponContentUnitRequest` 携带精炼等级而效果通道不携带。
- 随机源消费顺序冻结：先概率判定、后延迟抽样；`RandomSource.roll()` 在概率为 `0` 或 `1` 时不消耗序列，因此 R5 触发只消耗 1 次延迟抽样，与 R1–R4 的序列偏移不同。跨精炼比较测试必须按精炼分别固定种子。

原因：产球数量与元素取自 KQM TCL Energy「Favonius Series」条目，与[元素能量系统设计](../架构/系统/元素能量系统设计.md)第 3.1 节引用的能量资料基线同源；概率与间隔取自资产精炼文本，两者都不是本项目自造数值。把「恢复 6 点」落到既有微粒规则而不是新增直接回能通道，避免为单把武器扩出系统不具备的分支，也避免把显示文本当作公式真值。初版还把这五个精炼值抄进了内容代码，等于让资产数据出现第二份真值——资产侧修正不会反映到仿真，且没有任何校验会发现不一致；现已改为从资产参数读取，内容只保留「怎么读」的声明。概率抽样不消耗序列的行为会让 R5 与 R1–R4 的随机序列产生偏移，属易误判为回归的点，因此一并冻结。

承载：[元素能量系统设计](../架构/系统/元素能量系统设计.md)第 4.1 节、[元素能量系统契约](../契约/元素能量系统契约.md)、[资产数据库契约](../契约/资产数据库契约.md)第 8 节、[西风长枪实现规划](../工程/西风长枪实现规划.md)；实现落点 `backend/src/genshin_sim/content/weapons/polearm/favonius_lance/`，基准用例 `backend/tests/golden/energy/test_favonius_lance_windfall.py`。触发窗口状态归属见 D-078，参数解读责任划分见 D-080。

变更记录：
- 2026-09-19 新建条目，冻结顺风而行的产球形态、触发条件、判定粒度、精炼数值表、能量换算口径与随机源消费顺序。
- 2026-09-19 数值取值路径由「内容代码内硬编码精炼表」修订为「内容从资产效果参数按精炼读取」，精炼范围一并改由资产参数声明；冻结的数值本身不变。
