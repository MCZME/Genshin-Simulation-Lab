# UI 工程规范

> 状态：生效，随 UI 设计演进持续维护
> 最后更新：2026-08-17

## 1. 定位

本文定义 UI 模块的工程约束、文档组织与契约创建时机；产品与交互设计见[批处理画布设计](../架构/UI/批处理画布设计.md)，整体结构与状态设计见[UI 结构与状态设计](../架构/UI/UI结构与状态设计.md)。

UI 原型按[UI 原型验证规范](./UI原型验证规范.md)立项、验证和收敛。正式实现前必须阅读[UI 原型开发经验](./UI原型开发经验.md)，其中的经验与实现建议作为正式实现的参考基线；本文与结构设计文档负责把其中已确认的结论固化为结构规则，未确认问题继续留在原型文档中回答。

## 2. 模块边界

- `frontend/` 是独立 Web 前端工程，只通过 `infrastructure/http_api/` 的 HTTP API 调用 `application/services/` 的对外服务，不直接访问 SQLite，不组装仿真核心对象，不实现分析算法，不调用 CLI。
- 前端与 CLI 共享同一批应用服务；`application/` 不依赖任何前端实现或 UI 框架。
- `core/` 不感知 UI 状态；画布编辑状态不得泄漏到 `core/`。
- 技术栈已确认：Python + Web 前端（React + React Flow），桌面壳使用 pywebview 或浏览器；正式决策见[项目决策记录](../决策/项目决策记录.md) 2.28。

## 3. 分层约定

画布实现区分四个概念，落位如下（详细设计见[UI 结构与状态设计](../架构/UI/UI结构与状态设计.md)）：

- WorkflowDefinition / WorkflowValidator / WorkflowRuntime：`application/workflow/`，无 Flet 依赖，UI 与 CLI 可共享。
- WorkflowEditorState：`frontend/src/state/`，只装编辑期语义状态；视口、拖拽等瞬态留在组件内部。

UI 采用声明式结构，顶层原则为“全局声明式、命令式受控例外”。组件分为四类：

- 纯组件：state 进、控件出，无内部状态。
- 编辑器组件：持有本地瞬态，提交语义事件。
- 实例组件：持有控件实例/焦点/绘制上下文，对外声明式。
- 桥接组件：对接异步，只转发数据与回调。

状态层规则：

- AppState 管项目/服务/导航；EditorState 管语义状态；RunState 管运行期状态。
- 视口归画布实例组件持有；结果数据不进 UI 状态。
- 业务状态只有唯一更新入口，命令式不得绕过桥接层。

节点类型注册表分工：

- 语义注册表（节点类型、片段形状、端口、参数、区域约束）在 `application/workflow/`。
- 编辑器组件映射在 `frontend/`（React 组件注册表）。
- 显式注册，不自动扫描；未注册节点类型在加载/校验时报错。

具体边界与数据模型在实现阶段定稿，不预建万能框架。

## 4. 文档组织

- 产品形态与交互设计：`docs/架构/UI/`。
- 原型验证流程与验收规则：`docs/工程/UI原型验证规范.md`。
- 原型经验与实现建议：`docs/工程/UI原型开发经验.md`，正式实现前必读。
- 原型代码：`prototypes/` 下的代码只用于本地 UI 验证，不提交到仓库，不作为正式实现参考；正式实现只依据已确认文档与原型经验重新设计和编码。
- UI 工程规范：本文档（`docs/工程/`）。
- 契约文档：`docs/契约/`，进入实现时创建。

## 5. 契约状态

契约已在讨论定稿后创建于 `docs/契约/`，进入实现时按契约实现：

1. [工作流定义契约](../契约/工作流定义契约.md)（节点、连线、端口与片段应用语义）。
2. [节点类型与编辑器数据契约](../契约/节点类型与编辑器数据契约.md)。
3. [批量配置变体契约](../契约/批量配置变体契约.md)（配合 `application/batch/`）。
4. [UI API 契约](../契约/UI API 契约.md)。

## 6. 验证

- Python 侧改动按范围运行 `pytest`、`ruff`、`pyright`；前端改动运行 `pnpm lint`、`pnpm typecheck`、`pnpm test`；交互原型与文档改动不伪造未运行的检查结果。
- 状态、转换、校验、运行时等纯逻辑必须与前端框架解耦并纳入单测；组件层使用壳测试与人工/截图验证。
- 修改本规范或 UI 设计文档时，同步修正 `docs/文档入口.md` 与相关引用。
